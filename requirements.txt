# 5G-PacketSense Integrated - Requirements
# Install with: pip install -r requirements.txt

# ── Web Framework ──────────────────────────────────────────
flask>=3.0.0
flask-socketio>=5.3.5
flask-cors>=4.0.0
python-socketio>=5.10.0
eventlet>=0.34.2

# ── Packet Capture & Parsing ──────────────────────────────
scapy>=2.5.0
pyshark                         # tshark-based PCAP parsing (optional pipeline)

# ── Data Processing & Analytics ───────────────────────────
pandas>=2.0.0
pyarrow>=14.0.0                 # Parquet I/O with Snappy compression
duckdb>=0.9.0                   # In-memory SQL analytics engine
lxml                            # XML/PDML parsing

# ── Geolocation ───────────────────────────────────────────
maxminddb-geolite2              # Offline GeoIP database (MaxMind GeoLite2)
requests>=2.31.0                # HTTP client for ip-api.com fallback & VPN list downloads

# ── AI / LLM Integration (optional — needed for NL queries) ─
openai>=1.0.0                   # OpenAI / OpenRouter / Ollama (OpenAI-compatible API)
anthropic                       # Anthropic Claude
# google-generativeai           # Google Gemini (uncomment if needed)

# ── CLI & Utilities ───────────────────────────────────────
click                           # CLI command framework
tabulate>=0.9.0                 # Pretty-print tables in terminal
graphviz                        # Call-flow DOT graph rendering
tqdm                            # Progress bars

# ── Configuration ─────────────────────────────────────────
python-dotenv                   # Load .env files for API keys
